name: Upload CI Audit
description: Upload GitOrch CI audit reports
runs:
  using: composite
  steps:
    - name: Upload audit reports
      uses: actions/upload-artifact@v4
      with:
        name: gitorch-ci-audit
        path: |
          ci/audit/**
          playwright-report/**
        if-no-files-found: warn
